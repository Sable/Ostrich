Usage
    spmv_ostrich(5000,2000,0.01,100);
    spmv_ostrich_loop(5000,2000,0.01,100);

Files:
	spmvRun.m
    - spmv_ostrich.m
    - spmv_ostrich_loop.m

Note:
    - spmv_ostrich and spmv_ostrich_loop generate the same output, but
    different implementation from line [78] to line [83]
    - In C version, spmv takes 50000 other than 5000 as the first argument